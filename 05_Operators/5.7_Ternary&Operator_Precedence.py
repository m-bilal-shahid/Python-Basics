'''Ternary Operator:
"Ternary" means three parts.

In programming, a ternary operator is a shortcut way of writing an if-else statement in one line.

It takes 3 operands:

Condition

Value if condition is True

Value if condition is False'''



# Syntax in Python
# value_if_true if condition else value_if_false

#  Examples
# Example 1: Simple number check
# a = 10
# b = 20
# min_val = a if a < b else b
# print("Minimum is:", min_val)




# # Example 2: Even or Odd
# num = 7
# result = "Even" if num % 2 == 0 else "Odd"
# print(result)






'''
Python Operator Precedence & Associativity:
Parentheses () → sabse pehle evaluate hote hain. (Left → Right)

Exponentiation ** → Right → Left

Multiplication, Division, Floor Division, Modulus *, /, //, % → Left → Right

Addition, Subtraction +, - → Left → Right

Comparison & Equality <, <=, >, >=, ==, != → Left → Right

Logical NOT not → Right → Left

Logical AND and → Left → Right

Logical OR or → Left → Right

Assignment Operators =, +=, -=, *=, /=, %=, //=, **= ... → Right → Left'''